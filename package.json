{
  "name": "cryowire-viewer-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && vite build --config vite.config.content.ts && cp manifest.json dist/ && cp -r icons dist/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "~5.6.0",
    "vite": "^6.0.0"
  }
}
